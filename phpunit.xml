<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         failOnWarning="true"
         failOnRisky="true">

    <testsuites>
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>

    <php>
        <!-- Отдельная база: прогон тестов не затрагивает данные разработки.
             force обязателен: без него значение не перекрывает переменную,
             уже установленную в окружении контейнера. -->
        <env name="DB_DSN" value="pgsql:host=db;port=5432;dbname=gamer_market_test" force="true"/>
        <env name="DB_USER" value="gamer" force="true"/>
        <env name="DB_PASSWORD" value="secret" force="true"/>
        <env name="PROVIDER_URL" value="http://nginx/stubs/provider-a" force="true"/>
    </php>
</phpunit>
